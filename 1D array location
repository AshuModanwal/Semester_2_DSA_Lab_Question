#include <stdio.h>
int main()
{
    int arr[10];
    for (int i = 0; i < 10; i++)
    {
        scanf("%d", &arr[i]);
    }
    int *p = &arr[0];

   for(int i=0;i<10;i++)
   {
    printf("The address of element %d in the array is = %d\n"arr[i],(p+sizeof(int)*i));
   }

}
